import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

/**
 * Модуль ядра (хранит все общее для фронта и бэка)
 * @export
 * @class CoreModule
 */
@NgModule({
  imports: [CommonModule],
})
export class CoreModule {}
